supabase>=2.4.0
python-dotenv==1.0.0
# realtime==1.0.0  # supabase 최신 버전과 충돌 (realtime>=2.x 필요)
httpx>=0.27.0,<0.28.0

langchain==0.1.0          # ECS 배포 기준 고정 버전 (올리면 langchain-community 호환 확인 필요)
langchain-community==0.0.10

ollama>=0.3.0
openai>=1.30.0

tiktoken>=0.7.0
cryptography>=42.0.0

# HTTP API (ECS 배포용)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
